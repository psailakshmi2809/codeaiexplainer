// import { render } from '@testing-library/react';
// import React from 'react';
// import { Provider } from 'react-redux';
// import { BrowserRouter as Router } from 'react-router-dom';
// import store from '../store';

// import SettingsMenu from '../components/SettingsMenu';
// import AccountManagement from '../features/account-management/AccountManagement';
export {};

test('should render the SettingsMenu component.', () => {
  // TODO tests need to be debugged
  // const { getByText } = render(
  //   <Provider store={store}>
  //     <Router>
  //       <SettingsMenu />
  //     </Router>
  //   </Provider>,
  // );
  // const linkElement = getByText(/Manage users?/i);
  // expect(linkElement).toBeInTheDocument();
  expect(true).toBe(true);
});

test('should render the AccountManagement component.', () => {
  // TODO tests need to be debugged
  // const { getByText } = render(
  //   <Provider store={store}>
  //     <AccountManagement />
  //   </Provider>,
  // );
  // const linkElement = getByText(/MANAGE ACCOUNT?/i);
  // expect(linkElement).toBeInTheDocument();
  expect(true).toBe(true);
});
